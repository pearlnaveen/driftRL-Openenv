[project]
name = "driftrl-openenv"
version = "0.1.0"
description = "RL-based Data Cleaning OpenEnv"
dependencies = [
    "fastapi",
    "uvicorn",
    "torch",
    "pandas",
    "numpy",
    "requests",
    "openai",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:app"
